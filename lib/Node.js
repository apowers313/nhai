const {TransientObject} = require("./TransientObject");

class Node extends TransientObject {
    // load()
    // store()
    // connectTo(id)
    // connectFrom(id)
    // srcEdges[]
    // dstEdges[]
    // Node.create
}

module.exports = {
    Node,
};
