const {TransientObject} = require("./TransientObject");

class Edge extends TransientObject {
    // load()
    // store()
    // srcNodes[]
    // dstNodes[]
    // Edge.create()
}

module.exports = {
    Edge,
};
